server:
  port: 8189
  servlet:
    context-path: /security

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:

  datasource:
    url: "jdbc:postgresql://localhost:5432/db_first"
    username: "postgres"
    password: "123"
    driver-class-name: "org.postgresql.Driver"


